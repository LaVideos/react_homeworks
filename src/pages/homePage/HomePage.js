import React from 'react';
import {HomeComponent} from "../../components";

const HomePage = () => {
    return (
        <div>
            <HomeComponent/>
        </div>
    );
};

export default HomePage;