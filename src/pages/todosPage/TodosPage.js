import React from 'react';
import {TodosComponents} from "../../components";

const TodosPage = () => {
    return (
        <div>
            <TodosComponents/>
        </div>
    );
};

export default TodosPage;